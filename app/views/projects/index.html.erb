<%= render 'partials/header' %>

<div class="container">
  <div class="portfolio-list">
    <ul>
      <% @projects.each do |item| %>
        <li>
          <img class="portfolio-sample" src="/img/projects/<%= item.image %>">

          <h3><a href="<%= item.link %>"><%= item.title %></h3></a>

          <p><%= item.description %></p>

          <div class="clr-both"></div>
        </li>
      <% end %>
    </ul>
  </div>
</div>